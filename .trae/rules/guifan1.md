1.python程序都放在py文件夹下
2.每次不需要提交到git仓库，只需要提交到本地仓库即可
3.路劲都用相对路径，避免使用绝对路径
4.项目思路文件.txt不上传到git仓库
5.创建了临时文件使用后需要删除
6.项目中所有的服务器地址或者隐私信息，数据库配置文件，api_key，需要配置的内容信息都存储在一个变量文件（config.json）中，而且这个文件需要忽略到git仓库中并且需要新增一个config_example.json示例文件上传，忽略隐私信息，已经存在的配置不要修改
7.项目中所有的sql文件都整合在 database_mysql.sql ，删除其他的sql文件
8.不能更改config.json文件中的配置值，可以添加新的配置内容或者配置信息，但是不能更改原来字段的配置值
9.kimi_api_key是文本生成AI的秘钥，qwen_api_key是图片生成AI的秘钥，不准改动