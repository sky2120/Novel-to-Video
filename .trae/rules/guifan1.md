1.python程序都放在py文件夹下
2.数据库配置文件放在db_config.json
3.每次不需要提交到git仓库，只需要提交到本地仓库即可
4.路劲都用相对路径，避免使用绝对路径
5.项目思路文件.txt不上传到git仓库
6.例如api_key不上传到git仓库，需要新增一个example示例文件上传，忽略隐私信息
7.数据库配置文件db_config.json不上传到git仓库，需要新增一个example示例文件上传，忽略隐私信息